// ค่า null คือตัวแปรที่ไม่มีค่าใดๆเลย ไม่เท่ากัน 0 ไม่เท่ากับสตริงว่าง ไม่สามารถคำนวณใดๆได้ แต่ถ้าหากเปรียบเทียบจะเป็นค่า false

let a=null;
if(!a){
    alert("a is null")
}else{
    alert("a is not null")
}

// ค่า undefined คือตัวแปรที่ประกาศเอาไว้แต่ไม่ได้กำหนดค่าเริ่มต้น เช่น

let b;
alert(b);

//NaN (มาจาก Not a Number) หมายถึงการนำตัวแปรที่ไม่ใช่ตัวเลขไปคำนวณคณิตศาสตร์

let c = 10;
let d ="x";
alert(10-d);